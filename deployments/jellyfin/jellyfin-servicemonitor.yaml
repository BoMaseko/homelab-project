apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: jellyfin
  namespace: monitoring
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: jellyfin-exporter
  endpoints:
    - port: "9192"
      interval: "30s"

