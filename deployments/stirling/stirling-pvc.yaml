apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: stirling-pvc
  namespace: stirling
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
